{% extends 'base.html.twig' %}

{% block title %}ComprasProductos{% endblock %}

{% block body %}
    <h1>ComprasProductos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ compras_producto.id }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ compras_producto.cantidad }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('compras_productos_index') }}">back to list</a>

    <a href="{{ path('compras_productos_edit', {'id': compras_producto.id}) }}">edit</a>

    {{ include('compras_productos/_delete_form.html.twig') }}
{% endblock %}
