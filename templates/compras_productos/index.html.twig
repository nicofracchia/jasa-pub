{% extends 'base.html.twig' %}

{% block title %}ComprasProductos index{% endblock %}

{% block body %}
    <h1>ComprasProductos index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Cantidad</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for compras_producto in compras_productos %}
            <tr>
                <td>{{ compras_producto.id }}</td>
                <td>{{ compras_producto.cantidad }}</td>
                <td>
                    <a href="{{ path('compras_productos_show', {'id': compras_producto.id}) }}">show</a>
                    <a href="{{ path('compras_productos_edit', {'id': compras_producto.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('compras_productos_new') }}">Create new</a>
{% endblock %}
