{% extends 'base.html.twig' %}

{% block title %}AjustesStock{% endblock %}

{% block body %}
    <h1>AjustesStock</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ajustes_stock.id }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ ajustes_stock.fecha ? ajustes_stock.fecha|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ ajustes_stock.cantidad }}</td>
            </tr>
            <tr>
                <th>Observaciones</th>
                <td>{{ ajustes_stock.observaciones }}</td>
            </tr>
            <tr>
                <th>Stock_anterior</th>
                <td>{{ ajustes_stock.stockAnterior }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ajustes_stock_index') }}">back to list</a>

    <a href="{{ path('ajustes_stock_edit', {'id': ajustes_stock.id}) }}">edit</a>

    {{ include('ajustes_stock/_delete_form.html.twig') }}
{% endblock %}
