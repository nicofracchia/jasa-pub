{% extends 'base.html.twig' %}

{% block title %}Almacenes{% endblock %}

{% block body %}
    <h1>Almacenes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ almacene.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ almacene.nombre }}</td>
            </tr>
            <tr>
                <th>Direccion</th>
                <td>{{ almacene.direccion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('almacenes_index') }}">back to list</a>

    <a href="{{ path('almacenes_edit', {'id': almacene.id}) }}">edit</a>

    {{ include('almacenes/_delete_form.html.twig') }}
{% endblock %}
