<form method="post" action="{{ path('motivos_ajustes_stock_delete', {'id': motivos_ajustes_stock.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ motivos_ajustes_stock.id) }}">
    <button class="btn">Delete</button>
</form>
