{% extends 'base.html.twig' %}

{% block title %}CotizacionesProductos{% endblock %}

{% block body %}
    <h1>CotizacionesProductos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cotizaciones_producto.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cotizaciones_productos_index') }}">back to list</a>

    <a href="{{ path('cotizaciones_productos_edit', {'id': cotizaciones_producto.id}) }}">edit</a>

    {{ include('cotizaciones_productos/_delete_form.html.twig') }}
{% endblock %}
