{% extends 'base.html.twig' %}

{% block title %}Productos{% endblock %}

{% block body %}
    <h1>Productos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ producto.id }}</td>
            </tr>
            <tr>
                <th>Titulo</th>
                <td>{{ producto.titulo }}</td>
            </tr>
            <tr>
                <th>Codigo_barras</th>
                <td>{{ producto.codigoBarras }}</td>
            </tr>
            <tr>
                <th>Costo</th>
                <td>{{ producto.costo }}</td>
            </tr>
            <tr>
                <th>Porcentaje_costo</th>
                <td>{{ producto.porcentajeCosto }}</td>
            </tr>
            <tr>
                <th>Precio_final</th>
                <td>{{ producto.precioFinal }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ producto.descripcion }}</td>
            </tr>
            <tr>
                <th>Habilitado</th>
                <td>{{ producto.habilitado }}</td>
            </tr>
            <tr>
                <th>Eliminado</th>
                <td>{{ producto.eliminado }}</td>
            </tr>
            <tr>
                <th>Iamgen</th>
                <td>{{ producto.iamgen }}</td>
            </tr>
            <tr>
                <th>Stock_actual</th>
                <td>{{ producto.stockActual }}</td>
            </tr>
            <tr>
                <th>Stock_minimo</th>
                <td>{{ producto.stockMinimo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('productos_index') }}">back to list</a>

    <a href="{{ path('productos_edit', {'id': producto.id}) }}">edit</a>

    {{ include('productos/_delete_form.html.twig') }}
{% endblock %}
