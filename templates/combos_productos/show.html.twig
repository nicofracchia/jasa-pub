{% extends 'base.html.twig' %}

{% block title %}CombosProductos{% endblock %}

{% block body %}
    <h1>CombosProductos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ combos_producto.id }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ combos_producto.cantidad }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('combos_productos_index') }}">back to list</a>

    <a href="{{ path('combos_productos_edit', {'id': combos_producto.id}) }}">edit</a>

    {{ include('combos_productos/_delete_form.html.twig') }}
{% endblock %}
