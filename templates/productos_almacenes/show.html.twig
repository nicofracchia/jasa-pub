{% extends 'base.html.twig' %}

{% block title %}ProductosAlmacenes{% endblock %}

{% block body %}
    <h1>ProductosAlmacenes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ productos_almacene.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('productos_almacenes_index') }}">back to list</a>

    <a href="{{ path('productos_almacenes_edit', {'id': productos_almacene.id}) }}">edit</a>

    {{ include('productos_almacenes/_delete_form.html.twig') }}
{% endblock %}
