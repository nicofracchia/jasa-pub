{% extends 'base.html.twig' %}

{% block title %}ProveedoresAlmacenes index{% endblock %}

{% block body %}
    <h1>ProveedoresAlmacenes index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for proveedores_almacene in proveedores_almacenes %}
            <tr>
                <td>{{ proveedores_almacene.id }}</td>
                <td>
                    <a href="{{ path('proveedores_almacenes_show', {'id': proveedores_almacene.id}) }}">show</a>
                    <a href="{{ path('proveedores_almacenes_edit', {'id': proveedores_almacene.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('proveedores_almacenes_new') }}">Create new</a>
{% endblock %}
