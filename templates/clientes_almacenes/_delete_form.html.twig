<form method="post" action="{{ path('clientes_almacenes_delete', {'id': clientes_almacene.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ clientes_almacene.id) }}">
    <button class="btn">Delete</button>
</form>
