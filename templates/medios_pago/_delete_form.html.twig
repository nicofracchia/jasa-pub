<form method="post" action="{{ path('medios_pago_delete', {'id': medios_pago.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medios_pago.id) }}">
    <button class="btn">Delete</button>
</form>
